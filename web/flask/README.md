# MuktabahSyamilah on FLASK

cara run:
- copy folder data yang berisi numpy dan model di dalam folder data, letakkan forlder data di root folder (setera dengan file app.py)
- pip install -r requitements.txt
- python app.py